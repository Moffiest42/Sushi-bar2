@model Character

@{
    ViewData["Title"] = "Удалить";
}

<h1>Удалить</h1>

<h3>Вы уверенны что хотите удалить запись?</h3>
<div class="card">
    <div class="card-header">
        <h4>Информация о блюде</h4>
    </div>
    <div class="card-body">
        <dl class="row">
            <dt class="col-sm-3">Название:</dt>
            <dd class="col-sm-9">@Html.DisplayFor(model => model.Name)</dd>

            <dt class="col-sm-3">Тип:</dt>
            <dd class="col-sm-9">@Html.DisplayFor(model => model.Weapon)</dd>

            <dt class="col-sm-3">Уровень:</dt>
            <dd class="col-sm-9">@Html.DisplayFor(model => model.Level.Level_Number)</dd>

            <dt class="col-sm-3">Игрок:</dt>
            <dd class="col-sm-9">@Html.DisplayFor(model => model.Gamer.Name)</dd>
        </dl>
    </div>
</div>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Удалить" class="btn btn-danger" /> |
        <a asp-action="Index">Вернуться назад</a>
    </form>
</div>
